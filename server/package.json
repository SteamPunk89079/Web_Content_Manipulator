{
  "name": "server",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server",

    "clean_upload": "Remove-Item .\\uploads\\* -Recurse -Force",
    "clean_upload_windows": "rmdir /s /q uploads && mkdir uploads",
    "clean_output": "Remove-Item .\\output\\* -Recurse -Force",
    "clean_output_windows": "rmdir /s /q output && mkdir output",
    "clean_windows": "npm run clean_upload_windows && npm run clean_output_windows",
    
    "clean":"npm run clean_upload && npm run clean_output && npm run clean_windows",
    
    "dev": "nodemon server"
  },
  "type": "module",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "ffmpeg-static": "^5.2.0",
    "multer": "^2.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
